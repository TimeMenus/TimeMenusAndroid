<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.timemenus.android">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity
            android:name="com.timemenus.android.DashboardActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- <service android:name=""> -->
        <!--  -->
        <!-- </service> -->
        <!-- <receiver android:name=""> -->
        <!--  -->
        <!-- </receiver> -->
        <!-- <provider -->
        <!-- android:authorities="" -->
        <!-- android:name=""> -->
        <!--  -->
        <!-- </provider> -->

        <activity android:name="com.timemenus.android.MenuActivity" />
        <activity android:name="com.timemenus.android.LoginActivity" />
        <activity android:name="com.timemenus.android.AddItemActivity" />
        <activity android:name="com.timemenus.android.AdminActivity" />
        <activity android:name="com.timemenus.android.AddMenuToday"></activity>
    </application>

</manifest>